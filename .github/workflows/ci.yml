name: CI

on:
  push:
    branches:
        - master
        - feature/*
  pull_request:
    branches:
        - master
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-checks:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        check: [lint, format-check, type-check]
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
        cache-dependency-path: 'pyproject.toml'

    - name: Install dependencies
      run: pip install -e ".[dev]" --no-deps && pip install torch pytest pytest-cov pytest-mock ruff mypy pre-commit

    - name: Run ${{ matrix.check }}
      run: |
        case "${{ matrix.check }}" in
          lint)
            echo "## 🔍 Linting Results" >> $GITHUB_STEP_SUMMARY
            if make lint > lint_output.txt 2>&1; then
              echo "✅ **All linting checks passed!**" >> $GITHUB_STEP_SUMMARY
            else
              echo "❌ **Linting failed!**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "### Errors found:" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              cat lint_output.txt >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "💡 **Fix suggestion:** Run \`make lint-fix\` to auto-fix issues" >> $GITHUB_STEP_SUMMARY
              exit 1
            fi
            ;;
          format-check)
            echo "## 🎨 Code Formatting Check" >> $GITHUB_STEP_SUMMARY
            if make format-check > format_output.txt 2>&1; then
              echo "✅ **Code is properly formatted!**" >> $GITHUB_STEP_SUMMARY
            else
              echo "❌ **Formatting issues found!**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "### Format differences:" >> $GITHUB_STEP_SUMMARY
              echo '```diff' >> $GITHUB_STEP_SUMMARY
              cat format_output.txt >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "💡 **Fix suggestion:** Run \`make format\` to auto-format code" >> $GITHUB_STEP_SUMMARY
              exit 1
            fi
            ;;
          type-check)
            echo "## 🔎 Type Checking Results" >> $GITHUB_STEP_SUMMARY
            if make type-check > mypy_output.txt 2>&1; then
              echo "✅ **All type checks passed!**" >> $GITHUB_STEP_SUMMARY
            else
              echo "❌ **Type checking failed!**" >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "### Type errors found:" >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              cat mypy_output.txt >> $GITHUB_STEP_SUMMARY
              echo '```' >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "💡 **Fix suggestion:** Add type hints and fix type errors" >> $GITHUB_STEP_SUMMARY
              exit 1
            fi
            ;;
        esac

  test:
    runs-on: ubuntu-latest
    needs: quality-checks
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: '3.12'
        cache: 'pip'
        cache-dependency-path: 'pyproject.toml'

    - name: Install dependencies
      run: pip install -e ".[dev]" --no-deps && pip install torch pytest pytest-cov pytest-mock ruff mypy pre-commit

    - name: Run tests
      run: |
        echo "## 🧪 Test Results" >> $GITHUB_STEP_SUMMARY
        if make test > test_output.txt 2>&1; then
          echo "✅ **All tests passed!**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          # Extract test summary
          if grep -q "=" test_output.txt; then
            echo "### Test Summary:" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            grep -E "(passed|failed|error|skipped|=)" test_output.txt | tail -5 >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
          
          # Extract coverage summary
          if grep -q "TOTAL" test_output.txt; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Coverage Report:" >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
            grep -A 10 "Name.*Stmts.*Miss.*Cover" test_output.txt >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
        else
          echo "❌ **Tests failed!**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Test output:" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat test_output.txt >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "💡 **Fix suggestion:** Review failing tests and fix issues" >> $GITHUB_STEP_SUMMARY
          exit 1
        fi
