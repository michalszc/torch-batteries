# Data API

The public data contracts are also exported from `torch_batteries`.

`DatasetBundle` accepts singular or named datasets in every phase.
`BatchScheduleConfig` controls the batch order shared by train and validation.
Resolved loader fields use `DataLoaderCollection`.

::: torch_batteries.data
